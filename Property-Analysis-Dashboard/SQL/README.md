# SQL

This folder contains the SQL scripts used to create and query the property analysis data warehouse.

The SQL layer supports the dimensional model and provides the reporting view used by Power BI.
