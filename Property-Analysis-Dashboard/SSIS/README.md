# SSIS ETL

This folder contains the SSIS packages used to extract, transform and load the property and location data.

- `Load_Raw_Data.dtsx` – loads the source data into SQL Server staging tables.
- `Load_DW_Data.dtsx` – transforms the staged data and loads the dimensional data warehouse.
